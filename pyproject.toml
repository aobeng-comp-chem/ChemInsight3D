[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
# C901 = mccabe cyclomatic complexity. The project's contract is that no
# function exceeds the threshold below; see `ruff check --select C901 .`
select = ["C901"]

[tool.ruff.lint.mccabe]
max-complexity = 10
